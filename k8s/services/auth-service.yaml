apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: coursework
  labels:
    app: auth-service
    app.kubernetes.io/part-of: coursework
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
