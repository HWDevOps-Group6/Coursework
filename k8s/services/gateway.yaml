apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: coursework
  labels:
    app: gateway
    app.kubernetes.io/part-of: coursework
spec:
  type: ClusterIP
  selector:
    app: gateway
  ports:
    - name: http
      port: 80
      targetPort: 3000
