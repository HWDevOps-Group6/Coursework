apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: coursework
  labels:
    app: patient-service
    app.kubernetes.io/part-of: coursework
spec:
  type: ClusterIP
  selector:
    app: patient-service
  ports:
    - name: http
      port: 3003
      targetPort: 3003
